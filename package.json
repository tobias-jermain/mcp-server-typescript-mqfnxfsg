{
  "name": "mcp-server-typescript",
  "version": "1.0.0",
  "description": "A minimal MCP server template for Render",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch & node --watch dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.0",
    "@types/supertest": "^7.0.0",
    "supertest": "^7.2.0",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
